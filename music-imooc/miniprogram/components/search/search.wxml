<!-- components/search/search.wxml -->
<view class="container">
	<!-- 外面传过来的全局样式无法在组件内定义样式 -->
	<i class="ic sousuo find"></i>
	<input
	 type="text"
	 placeholder="{{placeholder}}"
	 placeholder-class="holder"
	 class="bar"
	 bindinput="onInput"
	/>
	<button class="search" bind:tap="onSearch">搜索</button>
</view>

